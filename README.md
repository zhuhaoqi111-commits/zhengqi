# 政企项目

一个基于Django框架开发的企业级应用项目，采用现代化的开发架构和最佳实践。

## 项目特色

- 🏗️ **模块化设计** - 清晰的项目结构，便于维护和扩展
- 🔧 **REST API** - 完整的API接口设计，支持前后端分离
- 🧪 **测试覆盖** - 完善的测试体系，确保代码质量
- 📦 **依赖管理** - 分环境依赖管理，开发生产分离
- 🚀 **部署友好** - 提供部署脚本和工具
- 📚 **文档完善** - 详细的开发文档和使用说明

## 项目结构

```
├── app/                    # 应用核心代码
│   ├── models/            # 数据模型定义
│   ├── views/             # API视图处理
│   ├── controllers/       # 页面控制器
│   ├── services/          # 业务逻辑服务
│   ├── utils/             # 工具函数库
│   └── config/            # 项目配置管理
├── migrations/            # 数据库迁移文件
├── tests/                 # 测试代码
│   ├── unit/              # 单元测试
│   ├── integration/       # 集成测试
│   ├── e2e/               # 端到端测试
│   └── fixtures/          # 测试数据
├── static/                # 静态资源文件
│   ├── css/               # 样式文件
│   ├── js/                # JavaScript文件
│   ├── images/            # 图片资源
│   └── fonts/             # 字体文件
├── templates/             # 模板文件
│   ├── base/              # 基础模板
│   ├── components/        # 组件模板
│   ├── pages/             # 页面模板
│   └── layouts/           # 布局模板
├── docs/                  # 项目文档
├── requirements/          # 依赖管理
│   ├── base.txt           # 基础依赖
│   ├── dev.txt            # 开发环境依赖
│   └── prod.txt           # 生产环境依赖
├── tools/                 # 工具脚本
│   ├── setup.py           # 项目设置脚本
│   └── deploy.py          # 部署脚本
├── envs/                  # 环境配置
│   └── .env.example       # 环境变量示例
└── README.md              # 项目说明
```

## 快速开始

### 环境要求

- Python 3.8+
- Django 4.2+
- 数据库（SQLite/PostgreSQL/MySQL）

### 安装步骤

1. **克隆项目**
   ```bash
   git clone <项目地址>
   cd 政企
   ```

2. **设置环境变量**
   ```bash
   cp envs/.env.example .env
   # 编辑.env文件，配置您的设置
   ```

3. **创建虚拟环境**
   ```bash
   python -m venv venv
   
   # Windows激活
   venv\Scripts\activate
   
   # Linux/Mac激活
   source venv/bin/activate
   ```

4. **安装依赖**
   ```bash
   pip install -r requirements/dev.txt
   ```

5. **数据库设置**
   ```bash
   python manage.py migrate
   python manage.py createsuperuser
   ```

6. **运行开发服务器**
   ```bash
   python manage.py runserver
   ```

7. **访问应用**
   - 网站首页: http://localhost:8000
   - 管理后台: http://localhost:8000/admin

## 开发指南

### 代码规范

- 使用Black进行代码格式化
- 使用Flake8进行代码检查
- 使用isort进行导入排序

### 运行测试

```bash
# 运行所有测试
pytest

# 运行特定测试
pytest tests/unit/

# 生成测试覆盖率报告
pytest --cov=app
```

### 部署

```bash
# 使用部署脚本
python tools/deploy.py
```

## 技术栈

- **后端框架**: Django 4.2+
- **前端框架**: Bootstrap 5
- **数据库**: SQLite/PostgreSQL
- **API**: Django REST Framework
- **测试**: pytest
- **部署**: Gunicorn + Nginx

## 贡献指南

1. Fork 项目
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 许可证

本项目采用 MIT 许可证 - 查看 [LICENSE](LICENSE) 文件了解详情

## 联系方式

- 项目维护者: [您的姓名]
- 邮箱: [您的邮箱]
- 项目地址: [项目Git地址]

## 更新日志

### v1.0.0 (2024-01-01)
- 初始版本发布
- 基础项目框架搭建
- 核心功能模块实现